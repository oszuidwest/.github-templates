---
# Container Registry Cleanup Workflow Template
# Source: oszuidwest/.github-templates
#
# This workflow deletes untagged container images from GitHub Container Registry.
# Runs weekly on Sunday at 3 AM UTC.
#
# Copy to: .github/workflows/cleanup-ghcr.yml
#
# Customization:
#   - SINGLE PACKAGE: Use the simple version (default)
#   - MULTIPLE PACKAGES: Uncomment the matrix version and list your packages

name: Cleanup Container Registry

on:
  schedule:
    # Weekly on Sunday at 3 AM UTC
    - cron: '0 3 * * 0'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

# === OPTION 1: Single Package (Default) ===

jobs:
  cleanup:
    name: Delete Untagged Images
    runs-on: ubuntu-latest
    timeout-minutes: 10

    permissions:
      contents: read
      packages: write

    steps:
      - name: Delete Untagged Versions
        uses: actions/delete-package-versions@v5
        with:
          package-name: ${{ github.event.repository.name }}
          package-type: 'container'
          delete-only-untagged-versions: 'true'
          min-versions-to-keep: 0
        continue-on-error: true

# === OPTION 2: Multiple Packages (Matrix Strategy) ===
# Uncomment and delete OPTION 1 above when using this

# jobs:
#   cleanup:
#     name: Delete Untagged Images
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#
#     permissions:
#       contents: read
#       packages: write
#
#     strategy:
#       matrix:
#         package:
#           - my-app
#           - my-app-full
#           - my-app-minimal
#       fail-fast: false
#
#     steps:
#       - name: Delete Untagged Versions (${{ matrix.package }})
#         uses: actions/delete-package-versions@v5
#         with:
#           package-name: ${{ matrix.package }}
#           package-type: 'container'
#           delete-only-untagged-versions: 'true'
#           min-versions-to-keep: 0
#         continue-on-error: true
